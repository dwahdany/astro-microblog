name = "blog-digest-worker"
main = "src/index.ts"
compatibility_date = "2024-12-01"

[triggers]
crons = ["0 8 * * *"]  # Daily at 8am UTC

[vars]
BLOG_URL = "https://blog.wahdany.eu"

# Secrets (add via: wrangler secret put BUTTONDOWN_API_KEY)
# BUTTONDOWN_API_KEY = "your-api-key"
